# Visual 8086 Assembly Simulator

A browser-based environment where users can write 8086 assembly code and see real-time visualization of what happens inside the 8086 CPU as the program executes.

## 🚀 Quick Start

### Backend Setup

```bash
cd backend
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
pip install -r requirements.txt
python main.py